// lib/config.dart
class AppConfig {
  // In dev, match your Node backend (same as React's backendUrl)
  static const String backendBaseUrl = 'http://localhost:4000';
  // When you deploy, change this to your server URL
}